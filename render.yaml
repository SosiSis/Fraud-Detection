services:
  - type: web
    name: fraud-api
    env: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: STARTUP_COMMAND
        value: api

  - type: web
    name: fraud-dashboard
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: STARTUP_COMMAND
        value: dashboard
      - key: API_BASE_URL
        fromService:
          type: web
          name: fraud-api
          property: url
